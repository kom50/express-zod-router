# express-zod-router

> **Declare once, validate everywhere.**

A FastAPI-style routing layer for Express that uses Zod schemas as a single source of truth for validation, TypeScript types, and API documentation.

## Install

```bash
npm install express-zod-router express zod
```

[rest of README unchanged]